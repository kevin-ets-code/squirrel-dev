# CLAUDE.md

Contexte projet pour les sessions Claude Code. Concis et factuel.
Pour la doc destinée à l'humain (guides pas-à-pas : ajouter un projet, déployer,
personnaliser le thème…), voir [`GUIDE.md`](GUIDE.md) — à **maintenir à jour** en
parallèle du code (voir « Règles de travail »).

## Projet

Portfolio personnel présenté comme un éditeur de code (thème sombre type VS Code).
Stack : **React + Vite**, déployé sur **Vercel** (build Vite standard). Dossier
`squirrel-dev`. **Pas de routeur** : app pilotée par onglets, vit à la racine `/`.
[`vercel.json`](vercel.json) ajoute un *rewrite SPA* (toute route → `index.html`)
pour que les URL inconnues passent par l'app, qui affiche alors une **page 404**.

## Architecture du contenu

Tout le contenu vient d'un **seul fichier de données** : [`src/projects.json`](src/projects.json).
Aucun contenu n'est codé en dur dans le React — pour ajouter/modifier du contenu,
on touche au JSON, pas au code. **Exceptions** (données distinctes, versionnées à
part) : le changelog a sa propre source [`src/changelog.json`](src/changelog.json)
(cf. « Changelog » plus bas) et les services la leur,
[`src/services.json`](src/services.json) — tableau d'objets
`{ id, title, tagline, description, deliverables[], stack[] (ids d'outils), forWho,
relatedProjects[] (ids de projets) }`, alimentant la page `services.md` et
`GET /services` (`stack` et `relatedProjects` peuvent être vides — rendu/API
gèrent le cas, ids inconnus ignorés).

Structure (clés racine de `projects.json`) :

- `profile` — identité : `name`, `role`, `tagline`, `location`, `email`, `links` (github, linkedin), `experiences[]` (objets `{ period, role, context, description }`, **ordre respecté, plus récent en haut**). Alimente l'onglet README (en-tête + section « Me contacter »), la page **À propos** (carte d'identité + timeline depuis `experiences`) et `GET /profile` (renvoie le profil entier → `experiences` inclus **sans code dédié**).
- `readme` — texte d'accueil en markdown : `intro`, `approach`, `navigation` (un champ vide masque sa section).
- `projects[]` — un objet par projet : `id`, `name` (slug nu, affiché avec l'extension `.json`), `type` (`"pro"` | `"perso"`), `year`, `status`, `title`, `oneliner`, `problem`, `solution`, `stack` (ids d'outils), `metrics[]` (optionnel), `highlights[]` (optionnel), `kinds[]` (natures du projet, **liste ouverte** : `"site-vitrine"`, `"webapp"`, `"app-native"`, `"automatisation"`…), `platforms[]` (objets `{ store, url }`, `store` **liste ouverte** : `"web"`, `"ios"`, `"android"`…), `repo`. **`demo` a été remplacé par `platforms[{ store: "web" }]`** — il n'existe plus.
- `tools{}` — **map `id` → métadonnées** : `{ label, category, color, logo, url, description }`. `category` (optionnel) = nom de la catégorie d'affichage dans le panneau Outils (groupes repliables) ; absent/vide = groupe « Autres ».

### Système d'ids outils

Les outils sont identifiés par un **id stable** (slug minuscule, tirets : `github-pages`),
**pas par leur nom**. Renommer le `label` ne casse donc pas les références.

- `stack` (dans chaque projet) contient des **ids**, ex. `["webflow", "javascript", "css"]`.
- `tools` mappe chaque id vers ses métadonnées. Seul `label` est requis ; le reste est optionnel.
- Partout où un outil s'affiche, l'id est résolu vers son `label` via `tools`. Un id absent de `tools` s'affiche **tel quel** (fallback, pas de crash) + warning console.
- Comptages et liens du graphe se basent sur l'**id**.

Logique de résolution/dérivation : [`src/lib/tools.js`](src/lib/tools.js) et [`src/lib/graph.js`](src/lib/graph.js).

## Les trois vues

L'état `view` dans [`src/App.jsx`](src/App.jsx) (`'ide' | 'graph' | 'api'`) pilote la vue active : chaque vue **remplace à la fois la sidebar** (panneau contextuel) **et la zone principale** (`<main className="editor">`). L'activity bar bascule entre elles — boutons Graph/API actifs selon `view`, panneaux IDE actifs selon `view==='ide' && panel===…`. Ajouter une vue = ajouter une valeur à `view`, un bouton (activity bar + drawer mobile `MobilePanelTabs`), une branche sidebar et une branche zone principale ; **ne pas inventer un autre mécanisme**.

- **IDE** — explorateur de fichiers (projets rangés en `pro`/`perso`) + onglets. **Toute page de contenu** (README, **À propos**, fiche projet, fiche outil, future page) a un toggle **Preview** (rendu lisible) / **Raw** (source brute) + bouton copier, mutualisé dans [`src/components/ContentPage.jsx`](src/components/ContentPage.jsx) et [`src/components/RawView.jsx`](src/components/RawView.jsx) — **pas re-codé par page**. Raw selon le format : **JSON** pour projet/outil, **markdown source** pour README/À propos. Onglet **README** en accueil.
  - **Page À propos** [`src/components/AboutView.jsx`](src/components/AboutView.jsx) (onglet `about`, fichier `about-me.md`) : page de contenu via `ContentPage`. Trois parties — (a) **carte d'identité** (`name`/`role` **dérivés du profil**, ligne méta `ABOUT_IDENTITY` « codée dans la page ») ; (b) **récit en prose** (`ABOUT_PROSE`, markdown) ; (c) **timeline d'expériences en cartes** lue depuis `profile.experiences` (**jamais en dur**, ordre du tableau). Prose, méta et **source Raw** centralisées dans [`src/lib/markdown.js`](src/lib/markdown.js) (`ABOUT_PROSE`, `ABOUT_IDENTITY`, `aboutToMarkdown(profile)`) — source unique partagée Preview/Raw. **Toujours visible** dans l'explorateur (fichier système, comme README), `fileCount` +1 ; ouverte par `openAbout` (App.jsx) + action palette « Ouvrir À propos ». Type `about` → ext `.md` dans [`fileName.js`](src/lib/fileName.js).
  - **Page CHANGELOG** [`src/components/ChangelogView.jsx`](src/components/ChangelogView.jsx) (onglet `changelog`, fichier `CHANGELOG.md`) : page de contenu via `ContentPage`. **Toujours visible** dans l'explorateur (fichier système, comme README/À propos), `fileCount` +1 ; ouverte par `openChangelog` (App.jsx) + action palette « Ouvrir le CHANGELOG ». Type `changelog` → ext `.md` dans [`fileName.js`](src/lib/fileName.js). **Preview = une carte par version** (en-tête version + `date` **si présente**) ; pour chaque **catégorie présente** dans `changes` une section colorée. **Raw** = markdown source (`changelogToMarkdown` dans [`markdown.js`](src/lib/markdown.js)). Le rendu **itère sur les clés présentes** (jamais `added/changed/fixed` en dur) → une catégorie future (`removed`, `deprecated`, `security`) s'affiche sans modif. Source de données + sémantique des catégories : cf. « Changelog » plus bas.
  - **Page Services** [`src/components/ServicesView.jsx`](src/components/ServicesView.jsx) (onglet `services`, fichier `services.md`) : page de contenu via `ContentPage`. **Toujours visible** dans l'explorateur (fichier système, comme README/À propos/CHANGELOG), `fileCount` +1 ; ouverte par `openServices` (App.jsx) + action palette « Ouvrir les Services ». Type `services` → ext `.md` dans [`fileName.js`](src/lib/fileName.js). **Source dédiée** [`src/services.json`](src/services.json) (tableau d'objets `{ id, title, tagline, description, deliverables[], stack[], forWho, relatedProjects[] }`). **Preview = une carte par service** (cartes en **grille 2 colonnes** sur desktop via `.service-grid`, repli 1 colonne ≤ 900px) : title + tagline, description, « Pour qui », prestations (`deliverables`), **outils** en pastilles `ToolLogo` **cliquables** (ouvrent la fiche outil via `openTool` ; ids `stack` résolus via la map `tools` de `projects.json` — **section omise si `stack` vide**) et **projets liés** en chips **cliquables** (`relatedProjects` = ids résolus vers `projects` via `openProject` ; ids inconnus **ignorés**, **section omise si vide**). **Raw** = markdown source (`servicesToMarkdown(services, tools, projects)` dans [`markdown.js`](src/lib/markdown.js)). Tout itère sur les données (rien en dur) ; `audit-conseil` cumule `stack` vide **et** `relatedProjects` vide (les deux sections sont alors omises). Données aussi exposées via `GET /services` (cf. vue API).
- **Graph** — canvas **React Flow** (`@xyflow/react`) auto-généré depuis `projects.json` : un nœud par projet, un nœud par outil unique (déduit des `stack`), un lien projet → outil. Outils partagés grossissent. Composants : [`src/components/GraphView.jsx`](src/components/GraphView.jsx), [`src/components/graphNodes.jsx`](src/components/graphNodes.jsx). **Non disponible sous 720px** (le code React Flow n'est alors pas chargé).
- **API** — **fausse API REST en lecture seule** (GET only) du portfolio, interrogée **côté client** : aucune vraie requête réseau, tout est calculé depuis `projects.json`. Icône activity bar = accolades `{ }` ([`BracesIcon`](src/components/icons.jsx)), **tabbable** (utilisable au clavier, contrairement au Graph en `tabIndex={-1}`). Disponible sur mobile (sidebar = liste) ; `selectApi` ne ferme pas le drawer (sidebar = contenu utile), mais `selectEndpoint`/`replayLog` le ferment pour révéler la console.
  - **Source unique = moteur** : [`src/lib/api-endpoints.js`](src/lib/api-endpoints.js) (`API_RESOURCES`, `API_ENDPOINT_COUNT`). Chaque endpoint porte `method`, `path` (template `/projects/:id`), `label`, `description`, `params[]` (énumère les valeurs d'un segment `:param` du path, même forme `{ param, values|derive, description }` que `query`, lu via `allowedValues` — **source des ids pour l'autocomplétion**), `query[]` (`{ param, values|derive, description }`), `example` (string ou `(data)=>string`) et **`handler({ params, query, data })` → `{ status, body }`** qui pioche dans `projects.json`. `data = { profile, projects, tools, changelog, services }`. Sidebar, doc, console **et** autocomplétion lisent cette **même** source — jamais de liste ni de logique dupliquée. `/stats` et tout comptage sont **dérivés** (jamais en dur).
  - **Moteur** [`src/lib/api-engine.js`](src/lib/api-engine.js) : `parseRequest` (méthode défaut GET, split `?`, path normalisé) → `matchRoute` (segments + `:param`) → validation query → `handler`. Renvoie `{ method, path, status, statusText, body }`. Ordre des status : méthode ≠ GET → **405** ; route inconnue → **404** ; valeur de query hors valeurs connues → **400** ; sinon **200** (le handler peut renvoyer **404** sur id inconnu). Erreurs = body `{ error, message }`. Helpers exportés réutilisés ailleurs : `httpStatusVariant(code)` (`green`/`warn` → variables `--status-*`), `allowedValues(def, data)`, `resolveExample(ep, data)`, `STATUS_TEXT`.
  - **Routes** : `GET /projects` (`?type=pro|perso`, `?kind=…` dérivé, `?stack=…` dérivé — id d'outil, match **dans** le tableau `stack[]` comme `kind` dans `kinds[]` ; filtres **combinables**), `GET /projects/:id`, `GET /tools` (`?category=…` dérivé), `GET /tools/:id`, `GET /profile`, `GET /changelog` (`?category=…` dérivé), `GET /services` (`?stack=…` dérivé), `GET /services/:id`, `GET /stats`. Valeurs des filtres dérivées via `allowedValues` (validation 400 gratuite) ; ajouter `stack` au `query[]` de `/projects` suffit pour que la doc le liste.
    - **`GET /changelog`** : enveloppe liste `{ count, results }`, alimentée par [`changelog.json`](src/changelog.json) (donc `apiData` inclut `changelog`). Sans filtre → toutes les versions (antéchronologiques). `?category=added` (valeurs = **catégories connues** `KNOWN_CHANGELOG_CATEGORIES`, validation 400 gratuite sur l'inconnu — une catégorie connue sans entrée est **valide** → `count: 0`) → **filtrage « réduit »** (`filterByCategory` dans [`changelog.js`](src/lib/changelog.js)) : ne garde que les versions contenant la catégorie, **et** réduit chaque entrée à cette seule catégorie dans `changes` (`version`/`date` conservés) ; `count` recalculé après filtre.
    - **`GET /services`** : enveloppe liste `{ count, results }`, alimentée par [`services.json`](src/services.json) (donc `apiData` inclut `services`). Sans filtre → tous les services. `?stack=<id d'outil>` (valeurs = slugs réellement présents dans les `stack[]` des services, dérivées via `uniqueServicesStack` → validation 400 gratuite sur l'inconnu) ne garde que les services dont la `stack` contient ce slug ; `count` recalculé après filtre. **`GET /services/:id`** → objet **brut** du service (404 sur id inconnu) ; les ids sont énumérés par `params[]` → autocomplétion héritée comme `/projects/:id`.
  - **Forme des réponses** : les endpoints **liste** (`/projects` + variantes filtrées, `/tools`, `/services`) renvoient une **enveloppe** `{ count, results }` (`count` = `results.length`, **dérivé**, calculé **après** les filtres). Les endpoints **objet unique** (`/projects/:id`, `/tools/:id`, `/services/:id`, `/profile`, `/stats`) renvoient l'objet **brut** (pas d'enveloppe — un `count` n'aurait pas de sens). Le rendu (doc Swagger, console, détail des logs) lit `body` tel quel → reflète la forme automatiquement.
  - **Zone principale = `ApiView`** [`src/components/ApiView.jsx`](src/components/ApiView.jsx) : une **barre d'onglets INTERNE** (`apiTab` = `'console' | 'doc' | 'logs'`) + le panneau actif (tabpanel ARIA). Ce sous-état est **local à la vue API**, **distinct du système d'onglets de fichiers IDE** (`tabs`/`activeTab`) — ne pas le confondre ni le polluer. Barre d'onglets [`src/components/ApiTabs.jsx`](src/components/ApiTabs.jsx) : même pattern ARIA Tabs qu'`EditorTabs` (`tablist`/`tab`, roving tabindex, ←/→/Home/End, Entrée/Espace, `aria-controls`).
    - **Console** [`src/components/ApiConsoleView.jsx`](src/components/ApiConsoleView.jsx) : `<form>` champ requête + Send (Entrée), puis ligne de statut colorée (`aria-live`) + corps JSON via **`RawView` réutilisé** (coloration, n° de ligne **et** bouton copier inclus). État vide propre avant la 1ʳᵉ requête. Le champ est une **combobox avec autocomplétion** (cf. ci-dessous) ; la zone réponse est inchangée.
      - **Autocomplétion** [`src/lib/api-autocomplete.js`](src/lib/api-autocomplete.js) : **fonction pure `getSuggestions(input, data)`** = seule logique de suggestion, **dérivée de la source unique** `api-endpoints.js` (`API_RESOURCES` → ressources racines ; routes `…/:param` → ids via le nouvel **énumérateur déclaratif `params[]`** des endpoints `:id`, lu par `allowedValues` exactement comme `query[]`) et de `projects.json` — **aucun path ni id en dur**, testable isolément. **v1 = path + ids** ; on s'arrête au `?` (la query string est **préservée** dans la complétion, jamais complétée) — la query viendra en v2 par une branche supplémentaire de `getSuggestions` (point d'extension prévu, sans réécriture). Chaque suggestion porte `{ value, apply (ligne complète résultante), kind, hint }` ; filtre **préfixe** insensible à la casse ; une suggestion identique au segment déjà complet est **masquée** (pas de miroir). Le composant `ApiConsoleView` n'en fait que l'affichage (listbox sous le champ, mêmes tokens CSS `--accent` que la palette) et le **clavier** : **↑/↓** naviguer, **Tab** compléter, **Entrée** = *compléter* si le dropdown est ouvert (jamais exécuter), sinon *exécuter* (form submit) ; **Échap** ferme sans vider. Compléter = `onChange(apply)`, **n'exécute jamais** (cohérent avec le reste de l'app). **Pas de focus trap** (Tab finit par sortir). Un `lastUserValueRef` distingue la frappe d'un **pré-remplissage externe** (clic endpoint sidebar / rejeu log) pour **ne pas ouvrir le dropdown** intempestivement.
    - **Documentation** [`src/components/ApiDocPanel.jsx`](src/components/ApiDocPanel.jsx) : rendu **style Swagger UI** (maison, aucune marque/asset), **généré depuis `API_RESOURCES`** (jamais de liste dupliquée). Encart d'intro « lecture seule / `GET` uniquement » (le **405** y est mentionné une fois, **pas répété par carte**). Sections par ressource, chaque endpoint = **carte dépliable** (`aria-expanded`) **teintée par sa méthode** via le token `--api-method-get` (= `--code-blue`, suit le thème ; `color-mix` faible % → discret en dark, **pas** `--accent` pour garder GET reconnaissable) : badge méthode + path + description. Au dépli, **deux bandes** (`.api-doc-band`, séparées) : **Paramètres** (path `:param` dérivés → « requis » ; `query[]` → « optionnel » + valeurs via `allowedValues` ; « Aucun paramètre » sinon) et **Réponses** = **tableau sémantique Code/Description** (codes **dérivés** par `responseCodes(ep)` : `200` toujours, `400` si `query`, `404` si `:param` ; couleur via `httpStatusVariant` → classes `.api-code-*`) suivi de l'**exemple de réponse** = vraie exécution `executeRequest` (sur `resolveExample`) rendue via `RawView`. Bouton **« Essayer dans la console »** par carte → appelle `onSelectEndpoint(ep)` = **le même `selectEndpoint` que la sidebar** (threadé App → `ApiView` → `ApiDocPanel`) : bascule `apiTab='console'` + pré-remplit l'input **sans exécuter** ; la doc n'exécute jamais rien inline. **Remplace l'ancien onglet IDE `api-doc`/`ApiDocView` (supprimé)** : la doc ne vit plus dans l'éditeur.
    - **Logs** [`src/components/ApiLogsPanel.jsx`](src/components/ApiLogsPanel.jsx) : historique **plein écran** (liste méthode + path + code coloré via `httpStatusVariant`, plus récent en tête) + **panneau de détail en SPLIT INTERNE** à droite. **Clic sur une ligne = ouvre le détail SANS ré-exécuter** (lit le `body` stocké dans le log + query via `parseRequest`, pur parsing) ; état de sélection **local au composant** (`selectedId`). Détail : statut, path complet, query, corps via `RawView` (+ copie), bouton **« Rejouer »** (= seul chemin de ré-exécution, remonte via `onReplay`=`replayLog`). Fermeture × + **Échap**, focus rendu à la ligne. Le split **reste dans la zone éditeur** (flex, jamais d'overlay/drawer) ; sous 900px il s'empile (CSS). Placeholder « Aucune requête » si vide.
  - **État** : remonté dans [`src/App.jsx`](src/App.jsx) (`apiInput`, `apiResult`, `apiLogs`, `apiFocusKey`, **`apiTab`**), **session only** (pas de `localStorage` — réservé aux préférences). `runApiRequest` exécute + empile un log (plus récent en tête, borné à 50) en **y stockant le `body`** (pour le détail des logs, sans ré-exécuter) ; `selectEndpoint(ep)` **pré-remplit** l'input avec `ep.example` sans exécuter, **bascule `apiTab='console'`** (+ focus via `apiFocusKey`) ; `replayLog(entry)` ré-exécute **et** bascule `apiTab='console'`.
  - **Sidebar** [`src/components/ApiPanel.jsx`](src/components/ApiPanel.jsx) : **une seule section, Endpoints** (groupés par ressource via [`Folder`](src/components/Folder.jsx), clic = pré-remplir + onglet Console). Documentation et Logs **ne sont plus dans la sidebar** (ce sont des onglets internes d'`ApiView`) — pas de duplication.
- **Panneau Tools** (sidebar) — liste des outils déduits des `stack`, **groupés par `category`** dans des dossiers repliables (composant partagé [`src/components/Folder.jsx`](src/components/Folder.jsx), même que les groupes pro/perso de l'explorateur). Groupes triés alphabétiquement par catégorie, « Autres » (sans `category`) toujours en dernier ; outils triés alphabétiquement par label dans chaque groupe. Catégories **dérivées de la donnée** (rien en dur). Clic → fiche outil dans un onglet. [`src/components/ToolsPanel.jsx`](src/components/ToolsPanel.jsx), [`src/components/ToolView.jsx`](src/components/ToolView.jsx).
- **Panneau Source Control** (sidebar) — faux **historique git** purement narratif (aucun vrai git). Liste les projets comme des « commits » triés par **année décroissante puis id** (stable) : hash court déterministe (dérivé de l'id), `name` (slug nu, sans extension) en message de commit, badge `pro`/`perso`, année en méta ; compteur de section = nombre de projets. Clic/Entrée sur un commit **appelle `openProject`** (même onglet que l'explorateur, pas de doublon, focus déplacé). Tout dérive de `projects.json`. Icône `git-branch` dans l'activity bar (sous la recherche) + onglet « Git » du drawer mobile. [`src/components/SourceControlPanel.jsx`](src/components/SourceControlPanel.jsx), dérivation dans [`src/lib/history.js`](src/lib/history.js).
- **Palette de commandes** (Ctrl/Cmd+K) — modale [`src/components/CommandPalette.jsx`](src/components/CommandPalette.jsx). Liste **auto-dérivée de `projects.json`** (projets ; outils via `buildTools`/map `tools`) + actions (README, **À propos**, Settings, toggle vue, toggle thème). Elle **n'appelle que** les callbacks d'ouverture existants (`openProject`, `openTool`, `openReadme`, `openAbout`, `openSettings`, `toggleView`) — **aucune logique re-codée**, mêmes gardes anti-doublon. Préfixe `>` = actions seules. Raccourci global + `toggleView` dans [`src/App.jsx`](src/App.jsx) ; point d'entrée mobile = bouton dans la title bar (le raccourci n'a pas de sens au doigt). **Modale ⇒ focus piégé dedans** (exception assumée, cf. « Règles de travail »). La palette porte aussi les **commandes fantômes** de la partie ludique (`> konami`, `> solve …`), jamais rendues dans la liste (cf. ci-dessous).

## Changelog

Source dédiée [`src/changelog.json`](src/changelog.json) : **tableau antéchronologique**
(version la plus récente en tête, `1.x` en bas). Chaque entrée : `version` (semver),
`date` (**optionnelle** — clé absente si inconnue, pas de `null`), `changes` (objet
n'incluant **que les catégories non vides**). Catégories supportées : `added`,
`changed`, `fixed` (extensible : `removed`/`deprecated`/`security` déjà prévues côté
libellés/couleurs). **Le passé rétroactif est sans `date`** (dates réelles inconnues) ;
la 1ʳᵉ version réellement datée = la 1ʳᵉ dont on connaît la date.

- **Sémantique des catégories** [`src/lib/changelog.js`](src/lib/changelog.js) — source unique : `CHANGELOG_CATEGORY_LABELS` + `categoryLabel(key)` (fallback capitalisé) ; `CHANGELOG_CATEGORY_TAGS` + `categoryTag(key)` (abréviations courtes `ADD/CHG/FIX/RM/DEP/SEC` ; fallback = clé MAJ tronquée à 3 car) ; `categoryColorVar(key)` (→ token `--changelog-<key>`, repli `--changelog-default`) ; **`KNOWN_CHANGELOG_CATEGORIES`** = `Object.keys(CHANGELOG_CATEGORY_LABELS)` (les 6 catégories **connues**) ; `uniqueChangelogCategories(changelog)` (union **dérivée** des clés réellement **présentes**) ; `filterByCategory(changelog, cat)` (filtrage « réduit », cf. API). Tokens CSS `--changelog-added/changed/fixed/default` dans `:root` de [`styles.css`](src/styles.css) (dérivés de la palette : `--status-green`, `--api-method-get`, `--status-warn`, `--fg-muted`).
  - **Connues vs présentes** : la **validation API** `?category` s'appuie sur `KNOWN_CHANGELOG_CATEGORIES` (une catégorie connue mais absente des données reste **valide** → `count: 0` ; seul l'inconnu → **400**). `uniqueChangelogCategories` est **conservé** mais sert désormais à la **légende** de la page (s'enrichit toute seule quand une catégorie apparaît).
- **Deux consommateurs, jamais de duplication de la liste** : la page `CHANGELOG.md` ([`ChangelogView.jsx`](src/components/ChangelogView.jsx)) et l'endpoint `GET /changelog` ([`api-endpoints.js`](src/lib/api-endpoints.js)) relisent tous deux `changelog.json`. La page et l'API **itèrent sur les clés présentes** dans `changes` — aucune catégorie supposée, ajout futur d'une catégorie pris en charge sans code.
- **Rendu Preview de la page** = **lignes denses pleine largeur, façon logs** (groupé par version) : en-tête version (monospace, `--accent`) + date à droite si présente, puis **une ligne par changement** = tag court coloré (monospace, largeur fixe alignée, `title` = libellé pour le tooltip) + texte en **police courante**. Une **légende** en tête (tag = libellé) dérivée de `uniqueChangelogCategories`. Le sens passe par le **texte** du tag, pas que par la couleur (a11y). Le **Raw** (markdown) est inchangé.
- **Volontairement omis** : les easter eggs / jeux n'apparaissent pas dans le changelog (cohérent avec leur nature cachée, comme leur absence de `GUIDE.md`).

## Partie ludique (jeux / easter eggs)

Sous-système caché, déclenché à la découverte. **Volontairement absent de [`GUIDE.md`](GUIDE.md)** : `GUIDE.md` est la doc de présentation, lue en premier, où l'on préserve l'effet de découverte ; le détail technique vit ici. (Le repo est public et le code expose tout — il ne s'agit pas d'un secret, juste de ne pas divulgâcher dans la doc d'accueil.) Plusieurs en-têtes de composants renvoient à cette section par « partie ludique ».

- **Déblocage en deux couches** — état dans [`src/lib/easterEggs.jsx`](src/lib/easterEggs.jsx) (contexte monté à la racine [`src/main.jsx`](src/main.jsx), persisté en `localStorage` sous `squirrel-dev:easter-eggs`, lecture défensive `readStored` → toujours un `DEFAULT_STATE` valide même si JSON absent/corrompu) :
  1. **Mode Jeux** (`easterEggUnlocked`) — débloqué par le **Konami Code** (écouteur clavier dans [`src/App.jsx`](src/App.jsx)) **ou** la commande fantôme `> konami`. Révèle le panneau **Jeux** + la page `konami-code.md`.
  2. **Jeu par jeu** (`gamesUnlocked[id]`) — chaque jeu se débloque en résolvant **son énigme** via `> solve <réponse>`.

  Flags de **victoire-récompense** dans le même objet, à plat : `snakeVictory`, `memoryVictory`, `squirrelVictory` (rétrocompat assurée par la lecture `!!parsed.x` : un état stocké ancien retombe sur `false`). Callbacks exposés : `unlockKonami`, `unlockGame(id)`, et la **primitive générique `unlockVictory(id)`** (écrit le flag `<id>Victory`) — `unlockSnakeVictory` subsiste en **alias** (rétrocompat d'API), pas de logique dupliquée.
- **Registre = source unique** [`src/games/registry.js`](src/games/registry.js) : `GAMES[]`, un objet par jeu — `id` (clé stable de `gamesUnlocked`), `label` (**révélé uniquement une fois le jeu débloqué**, ne doit jamais fuiter : ni page konami, ni panneau Jeux, ni palette), `emoji`, `riddle`, `solutions[]` (deux énigmes ne partagent jamais une solution), `component`. Helpers : `gameTabId`, `gameById`, `normalizeAnswer` (sans accents, minuscules, espaces réduits — appliqué des **deux** côtés), `gameIdForAnswer` (réponse → id de jeu) et **`victoryGameIdForAnswer`** (reconnaît `<solution> victory` en **dérivant** les alias des `solutions` + suffixe « victory » — zéro chaîne en dur). Le champ **`emoji` est actuellement inutilisé** (il alimentait le toast de victoire, retiré au profit du `🎮` du composant `Toast`) : **conservé pour resservir** (ex. titres des pages victory) — ne pas le prendre pour un oubli à câbler ou à supprimer.
- **Énigmes & commandes** ([`src/App.jsx`](src/App.jsx), `solveRiddle`) : `> solve <réponse>` débloque le jeu correspondant ; `> solve <jeu> victory` débloque la **page-récompense** sans rejouer la condition cachée. Le cas victoire est testé **en priorité** (avant `gameIdForAnswer`, sinon le mot-solution seul serait capturé par l'énigme), avec les mêmes gardes : **jeu débloqué requis** (sinon « Mauvaise réponse… », ne révèle pas la commande), **déjà débloqué** (neutre), succès (toast). Les toasts ([`src/components/Toast.jsx`](src/components/Toast.jsx)) s'appuient sur l'icône `🎮` du composant — **aucun emoji en dur dans la chaîne** (homogénéité).
- **Trois jeux & conditions de victoire** (rendu sur `<canvas>`, état « chaud » en refs, cf. en-têtes des composants) :
  - **Snake** [`src/components/games/SnakeGame.jsx`](src/components/games/SnakeGame.jsx) — « grille parfaite » (serpent occupant les `TOTAL_CELLS`).
  - **Memory** [`src/components/games/MemoryGame.jsx`](src/components/games/MemoryGame.jsx) — « partie parfaite » : `moves === pairs` (zéro erreur) **et** temps `≤ pairs × 2 × FLIP_MS × TOLERANCE` (seuil **dérivé** des constantes, jamais en dur).
  - **Squirrel** [`src/components/games/SquirrelGame.jsx`](src/components/games/SquirrelGame.jsx) — palier `WIN_SCORE` atteint → état `'won'` distinct de `'over'`.

  À sa victoire, chaque jeu **appelle `onVictory` via `onVictoryRef`** (ref synchronisée par effet → pas de prop/`gameId` périmé dans la boucle ou les closures) **en plus** d'afficher son **overlay in-component** (les deux coexistent). Côté App, `onVictory` est **généralisé** à tous les jeux : `() => unlockVictory(activeGameTab.gameId)` (plus de cas Snake en dur).
- **Pages-récompense persistantes** : [`VictorySnakeView`](src/components/VictorySnakeView.jsx), [`VictoryMemoryView`](src/components/VictoryMemoryView.jsx), [`VictorySquirrelView`](src/components/VictorySquirrelView.jsx) — prose statique via `ContentPage`/`MarkdownView` (onglets `victory-snake` / `victory-memory` / `victory-squirrel`, rendus dans [`src/App.jsx`](src/App.jsx)). N'apparaissent dans l'explorateur **que si leur flag est vrai**. Le **lore de Snake est finalisé** ; **Memory et Squirrel restent des placeholders** explicitement marqués « à finaliser » dans le source.
- **Affichage sidebar** ([`src/components/GamesPanel.jsx`](src/components/GamesPanel.jsx), [`src/components/Sidebar.jsx`](src/components/Sidebar.jsx)) : noms via le helper unique [`fileName`](src/lib/fileName.js) — `<Label>.exe` pour un jeu débloqué, `victory-*.md` pour les pages. Tant qu'un jeu est **verrouillé**, le panneau Jeux affiche un libellé **anonyme « Jeu n°N »** (ni label ni énigme ne fuitent).

## Conventions

- **Tout est piloté par `projects.json`** — rien en dur (nœuds du graphe, listes d'outils, contenu des fiches, positions… sont tous dérivés du JSON).
- **Logos** : dans [`public/logos/`](public/logos/), nommés d'après l'id (`public/logos/<id>.svg`) ; le champ `logo` y pointe (`"/logos/<id>.svg"`). Rendu forcé en **blanc** sur fond `color`.
- **Pastille outil** : composant unique [`src/components/ToolLogo.jsx`](src/components/ToolLogo.jsx). Avec logo → logo blanc sur fond `color` ; **sans logo → initiale du label** (majuscule, blanche, monospace, centrée) sur fond `color` (gris neutre `--tool-badge-fallback` si `color` vide). Réutilisé partout (sidebar carré arrondi, fiche/graphe cercle) — ne pas re-coder ce fallback ailleurs.
- **Thème** : couleurs et polices en variables CSS dans le `:root` de [`src/styles.css`](src/styles.css) (`--accent` teal, `--font-mono`, etc.). Le **mode clair** est un second jeu de variables sous `:root[data-theme='light']` (on surcharge des variables, pas de patch au cas par cas). `--tab-top-active` **dérive de `--accent`** et `--accent-status` en est une variante `color-mix` assombrie : ils suivent l'accent choisi au runtime. En revanche les **icônes projets** `--icon-pro` (**ambre** `#e0b341`) et `--icon-perso` (**teal** `#4ec9b0`) sont **fixes** — elles ne suivent **pas** l'accent ; `--icon-tool` (bleu) et `--icon-system` (`#6a9ce0`, bleu plus clair — **fichiers système de la sidebar** : README, konami-code, pages victory) aussi, **toutes fixes**. (Accent par défaut = teal ⇒ icône perso = accent par défaut, voulu.)
- **Nom de fichier & icône d'onglet dérivés de l'extension** : [`fileName.js`](src/lib/fileName.js) est la **source unique** des extensions (table `EXT_BY_TYPE`) — `fileName(type, base)` donne le nom affiché (sidebar/onglets/breadcrumb), `extension(type)` l'extension nue. La **couleur d'icône d'onglet** ([`EditorTabs.jsx`](src/components/EditorTabs.jsx), `tabIconColor`) est **dérivée de l'extension**, pas d'une liste de types en dur : `.md` → `--icon-system` (toute page `.md`, présente ou future), `.json` projets → `--icon-pro`/`--icon-perso`, `game` (.exe) → `--icon-perso`. **Fallback ultime = `--icon-system`** (var garantie existante) pour qu'un type imprévu reste visible — ne **jamais** retomber sur `var(--icon-${type})` (var possiblement inexistante → icône invisible, le bug d'origine). Le point de statut coloré reste **spécifique à l'explorateur** (fiches projet, gardé par `status`), pas dans les onglets.
- **Hash de commit (Source Control)** : dérivation **pure et déterministe** de l'`id` du projet (FNV-1a 32 bits tronqué à 7 hex) dans le helper réutilisable [`src/lib/history.js`](src/lib/history.js) (`shortHash`, `buildHistory`). Purement cosmétique, ne change jamais d'un reload à l'autre ; seul l'`id` le détermine (pas le titre). Ne pas re-coder ce hash ailleurs.
- **Copie presse-papier** : helper unique [`src/lib/clipboard.js`](src/lib/clipboard.js) (`copyText`, avec repli `execCommand`), utilisé par [`CopyButton`](src/components/CopyButton.jsx) (vue Raw) et [`CopyEmail`](src/components/CopyEmail.jsx) — ne pas re-coder la copie ailleurs.
- **Préférences utilisateur (thème clair/sombre + accent)** : état UI, **pas du contenu** → **jamais dans `projects.json`**. Persistées en `localStorage` (`pref:theme`, `pref:accent`) via le contexte [`src/lib/preferences.jsx`](src/lib/preferences.jsx) monté à la racine ([`src/main.jsx`](src/main.jsx)). Défaut thème = `prefers-color-scheme` tant qu'aucun choix explicite. UI dans l'onglet **Settings** ([`src/components/SettingsView.jsx`](src/components/SettingsView.jsx)), ouvert par l'engrenage en bas de l'activity bar. Le panneau **Outils** utilise l'icône `package` ; l'engrenage (`GearIcon`) est réservé aux Paramètres.
- **Page 404** : [`src/components/NotFound.jsx`](src/components/NotFound.jsx), rendu par [`src/App.jsx`](src/App.jsx) quand `window.location.pathname !== '/'` (toute route est inconnue, l'app n'a pas de routeur). Écran thématique (faux onglet `404.md`, bloc terminal `cat … → No such file or directory`, bouton « ← Retour à l'accueil » qui remet l'URL à `/` via `history.replaceState`). Style 100 % via variables de thème (clair + sombre), bloc « Page 404 » dans `styles.css`. La règle côté hébergeur est dans `vercel.json`.
- **Message console d'accueil** : [`src/lib/consoleGreeting.js`](src/lib/consoleGreeting.js) (`logConsoleGreeting`), appelé **une fois au niveau module** dans [`src/main.jsx`](src/main.jsx) (pas à chaque render). Email + liens **dérivés de `profile`** (`projects.json`), **rien en dur**, même source que le README. Seul log volontaire ; pas de bruit console en prod au-delà de ça (hors warning de fallback outil déjà existant).

## Règles de travail

- **Toujours vérifier que `npm run build` passe** avant de conclure un changement.
- **Maintenir [`GUIDE.md`](GUIDE.md) à jour** (doc humaine) quand un comportement décrit y change — en plus du code.
- **Respecter l'accessibilité clavier déjà en place** : pattern ARIA Tabs (`tablist`/`tab`/`tabpanel`, roving tabindex, flèches + Entrée/Espace), **pas de focus trap** (Tab finit toujours par sortir vers le navigateur), focus visible (`:focus-visible`), skip link. Voir la section « Accessibilité clavier » de `GUIDE.md`.
- **Exception focus trap** : la règle « pas de focus trap » vaut pour la **nav principale**. Une **modale temporaire** (la palette de commandes) y **déroge** : le focus est piégé dedans tant qu'elle est ouverte, puis **rendu** à l'élément précédent à la fermeture (Échap / clic dehors / exécution).
- Ne rien coder en dur qui devrait venir de `projects.json`.

## Commandes

```bash
npm run dev      # serveur de dev → http://localhost:5173
npm run build    # build de production → dist/
```
